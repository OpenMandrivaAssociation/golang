diff -up go/src/cmd/link/internal/ld/lib.go.omv~ go/src/cmd/link/internal/ld/lib.go
--- go/src/cmd/link/internal/ld/lib.go.omv~	2023-08-02 16:56:43.692230385 +0200
+++ go/src/cmd/link/internal/ld/lib.go	2023-08-02 16:58:11.191968100 +0200
@@ -1555,18 +1555,18 @@ func (ctxt *Link) hostlink() {
 			// On ARM64, the GNU linker will fail instead of
 			// generating COPY relocations.
 			//
-			// In both cases, switch to gold.
-			altLinker = "gold"
+			// In both cases, switch to lld.
+			altLinker = "lld"
 
 			// If gold is not installed, gcc will silently switch
 			// back to ld.bfd. So we parse the version information
 			// and provide a useful error if gold is missing.
 			name, args := flagExtld[0], flagExtld[1:]
-			args = append(args, "-fuse-ld=gold", "-Wl,--version")
+			args = append(args, "-fuse-ld=lld", "-Wl,--version")
 			cmd := exec.Command(name, args...)
 			if out, err := cmd.CombinedOutput(); err == nil {
-				if !bytes.Contains(out, []byte("GNU gold")) {
-					log.Fatalf("ARM external linker must be gold (issue #15696), but is not: %s", out)
+				if !bytes.Contains(out, []byte("LLD")) {
+					log.Fatalf("ARM external linker must be lld, but is not: %s", out)
 				}
 			}
 		}
